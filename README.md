# OurPaintHub

# Описание

## Наименование

**Веб-платформа "OurPaintHUB" для регистрации пользователей и их взаимодействия друг с другом, обмена файлами
и управления обучающими материалами**

## Предметная область
Веб-платформа **OurPaintHUB** - это система хранящая данный о регистрации пользователей, хранения и обмена файлами,
социального взаимодействия и обучающими материалами. Она сочетает в себе функции файлового
хранилища и совместной работы с файлами.
Основные цели системы:
- Регистрация и авторизация пользователей с разными ролями (админ, пользователь).
- Хранение проектов, файлов, метаданных, история изменений.
- Дружба, обмен проектами, совместные проекты.
- Текстовые и видеоматериалы, управляемые администраторами.
- FAQ для связи пользователей с администраторами.
- Логирование действий для аудита и модерации.


## Общие ограничения целостности

- Все внешние ключи (FOREIGN KEY) должны ссылаться на существующие записи.
- Нельзя создать проект, если пользователь-владелец не существует.
- Нельзя создать запись в логах для несуществующего пользователя или проекта.
- При удалении родительской записи должны выполняться каскадные действия или блокировки в зависимости от бизнес-логики
- Поле email в таблице users должно быть уникальным.
- ID во всех таблицах уникальны и автогенерируются.
- Одинаковые пары пользователей в friendship запрещены (user1, user2 уникальны как пара).
- Поля, участвующие в связях (user_id, project_id, receiver_id, changer_id, admin_id и др.), не могут быть NULL.
- Поля, критичные для бизнес-логики (email, password, project_name, role, type для документации и медиафайлов) не могут
  быть NULL.
- В friendship пользователь не может быть другом сам себе (user1 != user2).
- В shared проект не может быть передан самому владельцу (receiver_id != project.owner).
- Поле type для медиафайлов и документации должно содержать только допустимые значения.
- При удалении пользователя должны удаляться все его проекты, роли, профили, связи дружбы, записи логов и переданные
  проекты.
- При удалении проекта должны удаляться все изменения (project_changes), метаданные (project_meta), записи об обмене (
  shared).
- В таблице entity_logs действия можно записывать только для существующих пользователей и существующих сущностей (
  проектов, медиафайлов, документации).



# Пользовательские роли

## Для каждой роли - наименование, ответственность, количество пользователей в этой роли?

Роль Администратор.
Ответсвенность:
- Добавление и редактирование обучающих текстовых и видео материалов.
- Модерация пользовательского контента.
- Поддержка обратной связи в разделе FAQ.
  Количество пользователей: 1.

Роль Пользователь.
Ответсвенность:
- Регистрация и ведение профиля.
- Добавление файлов для обмена.
- Доступ к обучающим материалам.
- Доступ к приложению.
- Социальное взаимодейтсвие.
- Управление проектами.
- Получение поддержки в разделе FAQ.
  Количество пользователей: не ограничено.


# Технологии разработки

## Язык программирования

Мы используем Python для бэкэнд части, а конкретнее фреймворк Django. Для фронтэнд разработки воспользуемся React.

## СУБД

- PostgreSQL

##  Контейнер
- Docker


# Тестирование

- pytest - основной фреймворк для тестирования.

# Инструкция по запуску 
- Клонирование репозитория
``` bush
git clone github.com/ourpaintteam/ourpainthub

cd ourpainthub
docker compose up --build -d
#or
npm run dev # Из корневой директории

```
- docker compose up --build -d
- Работает только в локальной сети
